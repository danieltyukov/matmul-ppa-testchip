# Verification and analysis toolchain for matmul-ppa-testchip.
# Install into a repo-local virtualenv:
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
cocotb==2.0.1
numpy>=1.26
matplotlib>=3.8
pytest>=8.0
